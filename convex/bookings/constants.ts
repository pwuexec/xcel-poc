/**
 * Booking-related constants
 */

/**
 * Users can join a lesson session this many minutes before the scheduled start time
 */
export const EARLY_JOIN_MINUTES = 10;

/**
 * Users can join a lesson session up to this many minutes after the scheduled start time
 */
export const LATE_JOIN_MINUTES = 60;
